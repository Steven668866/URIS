# Legacy URIS VL Demo

The original monolithic Streamlit video reasoning application has been preserved here during the platform refactor.

Run it with:

```bash
streamlit run legacy/legacy_video_reasoning_app.py
```

The new root app (`app.py`) is the modular URIS home-robot interaction platform focused on UI, system orchestration, and performance instrumentation.
